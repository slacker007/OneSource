# OneSource Working Notes

This file is the timestamped scratchpad for the active loop. Update it during each task so another agent can recover mid-task state if a loop is interrupted.

## Notes

- `2026-04-17T20:41:58Z` Started a durable-memory workflow follow-up tied to `P0-04`. Read `SPEC.md`, `PRD.md`, `AGENTS.md`, `README.md`, inspected `git status`, and confirmed `NOTES.md` did not exist yet.
- `2026-04-17T20:41:58Z` Planned doc changes: add `NOTES.md` to required repo memory, require agents to read or create it at loop start, append timestamped working notes during each task, and record stopping-point context before loop end.
- `2026-04-17T20:42:59Z` Verified updated sections with `git diff --check` plus targeted `sed` or `nl` reads of `AGENTS.md`, `README.md`, `PRD.md`, and `NOTES.md`. Current stopping point: docs update is complete, next recommended product work remains `P0-01`, blockers are none.
- `2026-04-17T21:00:00Z` Started a new loop for the user request to complete the project. Per `AGENTS.md`, this loop is scoped to the next unchecked PRD item, `P0-01`, rather than claiming whole-project completion with most phases still open.
- `2026-04-17T21:00:00Z` Read `SPEC.md`, `PRD.md`, `AGENTS.md`, `README.md`, and `NOTES.md`, then inspected `git status`. Found an in-progress app scaffold already present in the worktree: Next.js app files, Tailwind styling, ESLint, Prettier, Vitest, Playwright, and a homepage shell/test.
- `2026-04-17T21:00:00Z` Next step is to verify the existing scaffold with `npm run lint`, `npm run build`, `npm test`, and `npm run e2e` before deciding what gaps remain for `P0-01`.
- `2026-04-17T21:05:00Z` Verification found an install-state problem rather than an app-code failure. `npm run lint` fell back to a global ESLint 6 config lookup, while `next`, `vitest`, and `playwright` were not found from project scripts.
- `2026-04-17T21:05:00Z` Root cause appears to be an inconsistent scaffold install: `node_modules` exists but `node_modules/.bin` does not, and `package-lock.json` still has the old package name `tmp-app` from the deleted temporary scaffold directory. Plan is to repair local dependencies with `npm install`, then rerun the full `P0-01` verification path.
- `2026-04-17T21:12:00Z` Ran `npm install` to repair the scaffold install state. This refreshed local dependency wiring and updated `package-lock.json` to the correct root package name `onesource`.
- `2026-04-17T21:12:00Z` Post-install verification results: `npm run build` passed and `npm test` passed, but `npm run e2e` failed because the Playwright Chromium binary was not installed yet. `npm run lint` returned only a warning because generated `coverage/` artifacts were being linted.
- `2026-04-17T21:15:00Z` Applied `P0-01` cleanup edits: updated `eslint.config.mjs` to ignore generated verification artifacts, added `npm run e2e:install` to `package.json`, and rewrote `README.md` so local setup and verification commands match the actual scaffold instead of the earlier pre-scaffold repo state.
- `2026-04-17T21:22:00Z` Installed Playwright runtime requirements with `npm run e2e:install` and `sudo npx playwright install-deps chromium`, then reran the full verification path. Final results: `npm run lint`, `npm run build`, `npm test`, and `npm run e2e` all passed; Playwright Chromium smoke coverage is now working against the local app.
- `2026-04-17T21:25:00Z` Updated `PRD.md` to mark `P0-01` complete and set the next recommended item to `P0-02`. Current stopping point: prepare a commit for the completed scaffold item without including unrelated user-owned changes such as the separate `SPEC.md` formatting diff.
- `2026-04-17T20:54:37Z` Started `P0-02` for the user request to continue toward project completion. Re-read repo memory, confirmed the next unchecked item is the compose/env slice, and scoped the acceptance criteria to: compose-managed web + PostgreSQL + worker startup, boot-time env validation, and updated setup docs.
- `2026-04-17T20:54:37Z` Implementation plan for `P0-02`: add a committed `.env.example`, define a typed env schema, wrap app startup so invalid env fails before boot, add a lightweight worker process for the compose stack, introduce Dockerfile and `docker compose` config, and make Playwright configurable so a smoke test can target the compose-managed app.
- `2026-04-17T21:00:17Z` Applied the first `P0-02` code slice: added `zod` and `pg`, created `.env.example`, `Dockerfile`, `.dockerignore`, `docker-compose.yml`, an app instrumentation hook for boot-time env validation, a database-backed `/api/health` route, a placeholder worker process, and an env parser unit test. Also updated Playwright so it can target either a self-started local server or an already-running compose-managed app.
- `2026-04-17T21:00:17Z` Next step is verification in two layers: first the repo-local checks (`lint`, `test`, `build`, and browser smoke), then compose-managed startup plus a Chromium smoke run against the live compose stack.
- `2026-04-17T21:11:03Z` Verification status for `P0-02`: host-side `npm run lint`, `npm test`, `npm run build`, and `npm run e2e` all passed after the env/runtime changes. Compose verification also passed: `docker compose up --build -d` started PostgreSQL, web, and worker successfully; `docker compose ps` reported the stack healthy; `/api/health` returned HTTP 200 with a healthy database payload; and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm run e2e` passed against the compose-managed app.
- `2026-04-17T21:11:03Z` Debugging notes captured during compose verification: container builds initially failed because in-container npm registry access was refused, so the Dockerfile was changed to reuse the already-installed local dependency tree; the build then failed on remote Google font fetches, so the layout moved to local CSS font stacks; and Docker Desktop rejected the original named Postgres volume, so persistence moved to `./.docker/postgres-data`.
- `2026-04-17T21:11:03Z` Current stopping point: update git status, tear down the compose stack, and create the non-amended commit for completed checklist item `P0-02`. Next recommended product work is `P0-02a` for compose-based test execution workflows.
- `2026-04-17T21:16:35Z` Started `P0-02a`. Re-read repo memory, confirmed the next unchecked item is the compose-based test workflow slice, and restated the acceptance criteria as containerized unit, integration-ready, and Playwright Chromium verification without a manually bootstrapped host environment.
- `2026-04-17T21:16:35Z` Initial design decision for `P0-02a`: try to replace the host-dependent Docker build with container-native dependency installs, add a dedicated compose test runner service plus a Playwright profile backed by the official browser image, and update repo docs plus PRD handoff around the exact containerized verification commands.
- `2026-04-17T21:19:09Z` Applied the first `P0-02a` infrastructure slice: added a compose `test` runner, a profile-gated `playwright` service, and durable testing docs under `docs/testing.md` plus `docs/research/2026-04-17-playwright-docker-workflows.md`. The first attempt also switched the Dockerfile to in-container dependency installs, but that change had to be rolled back later when container access to `registry.npmjs.org` failed.
- `2026-04-17T21:19:09Z` Host verification passed before container verification: `npm run lint`, `npm test`, `npm run build`, and `npm run e2e` all succeeded. Next step is the compose-native verification path required by `P0-02a`.
- `2026-04-17T21:29:27Z` Compose verification results: `docker compose --profile test run --rm --build test run lint`, `docker compose --profile test run --rm test`, `docker compose --profile test run --rm test run build`, and `docker compose --profile test up --build --abort-on-container-exit --exit-code-from playwright playwright` all passed. The Playwright container successfully ran Chromium against the compose-managed web app.
- `2026-04-17T21:29:27Z` Important blocker discovered while trying to finish `P0-02a`: Docker containers in this environment cannot reach `registry.npmjs.org`. Both in-image `npm ci` and `npm install` failed with `ECONNREFUSED` plus `Exit handler never called!`, so the compose images still have to copy the already-installed local `node_modules` tree. This means the compose workflows are verified but `P0-02a` remains incomplete because the host-bootstrap-free requirement is still unmet.
- `2026-04-17T21:29:27Z` Current stopping point: durable docs and PRD handoff need to preserve the verified compose commands plus the registry-access blocker. Next recommended work remains `P0-02a` unless the requirement is relaxed; otherwise the next implementation target after that is `P0-03`.
- `2026-04-17T21:33:00Z` Started a new loop for the user request to complete the project. Re-read repo memory, inspected `git status`, and confirmed the repo is still in Phase 0 with `P0-02a` and `P0-03` open plus all later phases untouched.
- `2026-04-17T21:33:00Z` Revalidated the `P0-02a` blocker before changing scope. Host and container fetches to `https://registry.npmjs.org/` both failed, container DNS resolution still worked, and a fresh `docker build` stalled at `npm ci`, so the no-host-bootstrap acceptance criteria remain unmet in the current environment.
- `2026-04-17T21:33:00Z` Switched to the next unblocked Phase 0 item, `P0-03`, with acceptance criteria to ensure `README.md` truthfully documents setup, stack choices, layout, daily commands, compose workflows, and verification commands, and to create `docs/architecture.md` plus `docs/runbook.md` with accurate initial system context.
- `2026-04-17T21:38:00Z` Added `docs/architecture.md` and `docs/runbook.md`, updated `README.md` to point at the new durable docs, and updated `PRD.md` to mark `P0-03` complete while keeping `P0-02a` blocked on the unresolved npm registry network issue.
- `2026-04-17T21:38:00Z` Verification for `P0-03`: `[ -f docs/architecture.md ] && [ -f docs/runbook.md ] && echo ok`, `docker compose config >/tmp/onesource-compose-config.out && echo ok`, `git diff --check`, plus targeted `rg`/`sed` reads of `README.md`, `docs/architecture.md`, and `docs/runbook.md` to confirm the documented commands and file references match the repo.
- `2026-04-17T21:38:00Z` Current stopping point: create the non-amended commit for completed item `P0-03`. Next recommended work remains `P0-02a`, but it is still blocked until container/package-registry connectivity is fixed or the acceptance criteria are changed.
- `2026-04-17T21:49:07Z` Started a new loop for the user request to complete the project. Re-read repo memory, inspected `git status`, and confirmed the project is still far from full completion; the active unchecked checklist item remains `P0-02a`.
- `2026-04-17T21:49:07Z` Restated `P0-02a` acceptance criteria for this loop: compose-based test execution, including Chromium Playwright, without relying on a manually bootstrapped host environment such as a preinstalled repo-local `node_modules` tree.
- `2026-04-17T21:49:07Z` Revalidated the environment rather than trusting older blocker notes. Result: host `curl` can reach `https://registry.npmjs.org/`, but Node-based requests from both the host runtime and Docker containers still fail with `ECONNREFUSED`, and Docker bridge networking cannot fetch npm packages directly.
- `2026-04-17T21:49:07Z` Verified that `npm ci --offline` succeeds from the existing host npm cache. Built a proof-of-concept reduced offline cache by copying only the tarball cache entries referenced by the installed dependency set from `~/.npm/_cacache`; the reduced cache is about `194M` and successfully supports a fresh `npm ci --offline` in a temporary directory without using the repo-local `node_modules` tree.
- `2026-04-17T21:55:28Z` Applied the `P0-02a` implementation: added `vendor/npm-offline-cache.tar.gz`, `scripts/refresh-offline-npm-cache.mjs`, and a `cache:npm:refresh` package script; changed the Docker build to run `npm ci --offline`; ignored repo scratch directories in `.dockerignore` and `.gitignore`; and updated `README.md`, `docs/testing.md`, `docs/architecture.md`, `docs/runbook.md`, and `PRD.md` so the offline compose workflow is the documented source of truth.
- `2026-04-17T21:55:28Z` Verification passed for the completed `P0-02a` item. Host checks passed: `npm run cache:npm:refresh`, `npm run lint`, `npm test`, `npm run build`, and `npm run e2e`. Compose checks passed: `docker compose --profile test run --rm test run lint`, `docker compose --profile test run --rm test`, `docker compose --profile test run --rm test run build`, and `docker compose --profile test up --build --abort-on-container-exit --exit-code-from playwright playwright`. Final hygiene checks passed: `docker compose down --remove-orphans` and `git diff --check`.
- `2026-04-17T21:55:28Z` Follow-up cleanup: the first cache archive build emitted tar timestamp warnings inside Docker, so the refresh script was updated to create a deterministic archive with fixed metadata. Re-ran `npm run cache:npm:refresh` plus `docker compose --profile test run --rm test run lint`; the archive regenerated cleanly at about `193M` and the compose image path no longer emitted timestamp warnings.
- `2026-04-17T21:55:28Z` Current stopping point: `P0-02a` is now complete and the next recommended item is `P1-01` for the initial Prisma/auth/audit schema. Outstanding unrelated worktree changes still exist in `SPEC.md` and `docker-compose.yml`; they were treated as user-owned context and not reverted in this loop.
- `2026-04-17T22:05:42Z` Started `P1-01` for the user request to continue toward project completion. Re-read repo memory, restated the acceptance criteria as: Prisma-backed auth and audit tables with migration history plus seed-safe defaults, verified by Prisma validate, migration generation, and seed execution.
- `2026-04-17T22:05:42Z` Resolved environment viability before editing: raw Node HTTP fetches to the npm registry still failed, but `npm` package installs worked. Installed `prisma@6.19.3`, `@prisma/client@6.19.3`, and `dotenv`, then chose Prisma 6 for a simpler stable baseline while still adopting the new `prisma.config.ts` path to avoid the deprecated `package.json#prisma` config warning.
- `2026-04-17T22:06:55Z` Implemented the `P1-01` slice: added `prisma/schema.prisma`, `prisma/seed.mjs`, `prisma/system-roles.mjs`, `prisma.config.ts`, `src/lib/prisma.ts`, and `src/lib/auth/system-roles.ts` plus a Vitest coverage file for the seeded role catalog. Generated and applied migration `20260417220638_init_auth_audit`, which creates organizations, users, roles, user-role joins, Auth.js-compatible accounts/sessions/verification tokens, and audit logs.
- `2026-04-17T22:13:14Z` Durable docs updated for the new baseline: `README.md`, `docs/architecture.md`, `docs/runbook.md`, `docs/testing.md`, and new `docs/security.md` now document Prisma workflows, the seed defaults, and the current auth/audit security posture. Added `docs/research/2026-04-17-prisma-auth-baseline.md` to capture the Prisma config and Auth.js schema guidance used in this loop.
- `2026-04-17T22:13:14Z` Verification passed for `P1-01`: `npm run cache:npm:refresh`; `npx prisma format`; `npm run prisma:validate`; `npm run prisma:migrate:dev -- --name init_auth_audit`; `npm run db:seed`; `npm run lint`; `npm test`; `npm run build`; `npm run e2e`; `docker compose --profile test run --rm --build test run lint`; `docker compose --profile test run --rm --build test`; `docker compose --profile test run --rm --build test run build`; `docker compose --profile test up --build --abort-on-container-exit --exit-code-from playwright playwright`; `docker compose down --remove-orphans`; and `git diff --check`. Current stopping point: update `PRD.md`, then create the non-amended commit for completed checklist item `P1-01`.
- `2026-04-17T22:20:00Z` Started `P1-02` after re-reading repo memory and confirming it is the next unchecked PRD item. Acceptance criteria for this loop: one canonical opportunity must persist relationships to an agency, contract vehicle, source metadata, import lineage, search lineage, sync history, raw source payload retention, and competitor records; verification will require a Prisma migration plus seeded relational examples.
- `2026-04-17T22:20:00Z` Scoped the schema boundary to avoid silently completing `P1-02a`. This loop will add the core opportunity, lineage, saved-search, search-execution, sync-run, and competitor persistence needed for `P1-02`, but it will deliberately leave connector capability/config metadata and second-source scenario proof for the separate `P1-02a` checklist item.
- `2026-04-17T22:31:00Z` Implemented the `P1-02` Prisma slice. Added agencies, contract vehicles, opportunities, opportunity-vehicle joins, competitors, opportunity-competitor joins, source saved searches, source search executions, source sync runs, source records, and search/sync lineage join tables. Kept `sourceSystem` as a string field to preserve source-agnosticity, but intentionally deferred connector capability/config metadata for `P1-02a`.
- `2026-04-17T22:31:00Z` Reworked seeding around a deterministic scenario module so the same imported opportunity seed proves agency, vehicle, competitor, search lineage, sync lineage, and raw/normalized/import-preview payload retention. Moved the new fixture test under `src/lib/opportunities/` after discovering the Vitest include pattern only covers `src/**/*.test.{ts,tsx}`.
- `2026-04-17T22:31:00Z` Verification status for `P1-02` is currently green: `npx prisma format`; `npm run prisma:validate`; `docker compose up -d db`; `npm run prisma:migrate:dev -- --name add_opportunity_lineage_schema`; `npm run db:seed`; `npm run lint`; `npm test`; `npm run build`; `npm run e2e`; `docker compose --profile test run --rm --build test run lint`; `docker compose --profile test run --rm --build test`; `docker compose --profile test run --rm --build test run build`; `docker compose --profile test up --build --abort-on-container-exit --exit-code-from playwright playwright`; and a direct Prisma query confirming the seeded opportunity links to agency, vehicles, competitors, source record, search execution, and sync run.
- `2026-04-17T22:41:00Z` Started `P1-02a` after re-reading repo memory and confirming it is the next unchecked PRD item. Acceptance criteria for this loop: source-agnostic connector configs plus promotion-decision persistence that can support `sam.gov`, `usaspending_api`, and session-backed `gsa_ebuy` without schema rewrites, verified by schema examples for `sam.gov` plus one concrete second-source scenario.
- `2026-04-17T22:44:00Z` Implemented the `P1-02a` schema slice. Added `source_connector_configs`, `source_record_attachments`, `source_record_contacts`, `source_record_awards`, and `source_import_decisions`, then threaded optional connector-config relations through saved searches, search executions, sync runs, and source records while preserving the existing string `sourceSystem` history fields.
- `2026-04-17T22:47:00Z` Expanded the deterministic seed scenario into a multi-source proof. The seed now creates connector configs for `sam.gov`, `usaspending_api`, and `gsa_ebuy`; preserves `sam.gov` attachments and contacts; persists a `CREATE_OPPORTUNITY` import decision for the primary source record; and persists a `usaspending_api` award-enrichment source record plus a `LINK_TO_EXISTING` import decision against the same canonical opportunity.
- `2026-04-17T22:50:00Z` Verification passed for `P1-02a`: `npx prisma format`; `npm run prisma:validate`; `npm test`; `docker compose up -d db`; `npm run prisma:migrate:dev -- --name add_source_connector_metadata`; `npm run db:seed`; `npm run lint`; `npm run build`; `npm run e2e`; `node --input-type=module -e 'import { PrismaClient } from "@prisma/client"; ...'`; `docker compose --profile test run --rm --build test run lint`; `docker compose --profile test run --rm --build test`; `docker compose --profile test run --rm --build test run build`; `docker compose --profile test up --build --abort-on-container-exit --exit-code-from playwright playwright`; `docker compose down --remove-orphans`; and `git diff --check`.
- `2026-04-17T22:50:00Z` Current stopping point: durable docs and PRD handoff are updated for the connector-metadata baseline. Next recommended item is `P1-03` for opportunity workspace execution persistence; the unrelated user-owned `SPEC.md` worktree change remains untouched.
